# x86-64 VM modules

Crown files that emit GAS x86-64 assembly (inside Crown string literals). Loaded by `press/main.cr` when building `crown-x86_64`. Other platforms (e.g. arm64) would get their own `vm/<arch>/` later.
